services:
  - type: web
    name: furchat-bot
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python webhook_bot.py
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
